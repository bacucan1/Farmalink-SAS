# Con el proxy de Vite configurado en vite.config.ts,
# las peticiones /api van directo desde el mismo origen (sin Mixed Content)
# Dejar vacío para usar rutas relativas (/api/...) que Vite proxea internamente
VITE_API_URL=
